# 更新日志

本文件记录 FlFund 各版本的主要更新内容。发布新版本时，GitHub Actions 会自动提取对应版本的章节作为 Release 页面的说明。

格式约定：每个版本以 `## 版本号` 开头（不带 `v` 前缀），与打的 tag（如 `v1.8.0`）对应。

## 1.8.0

### 新增
- （在此填写本版本新增的功能）

### 优化
- （在此填写本版本的优化项）

### 修复
- （在此填写本版本修复的问题）
